# PhiEdit 与极简四轨框架

当前需求：安装 PhiEdit；提供固定四轨、无美工的 Unity 音游框架；不制作或内置用户展示谱面；用户自行写谱，导出 JSON 后简单导入即可运行。

## 已交付

- Re:PhiEdit 1.3.0 + Mod 2.4.0 离线便携版，中文界面。原版的联网检查在本机失败，最终使用维护者提供的离线版；详见 tools/安装来源.md。
- 根目录“打开 PhiEdit”和“打开 Unity 项目”快捷方式，以及《开始使用.md》。
- Unity 2022.3.62f3c1 工程，顶部“音游 → 导入 PhiEdit 谱面…”窗口。支持选择/拖入 JSON 和音频、自动找音频、复制导入、重复导入更新、导入后自动进入 Play 并开始演奏。
- RPE JSON：单条固定判定线、上方 Tap/Hold、四个横向区域。节拍按 BPMList 换算为秒，加上 META.offset 毫秒；支持变 BPM、跨 BPM 长按、负偏移提前音符。
- 游戏：A/S/D/F 四轨，单键/同时按键/长按，Perfect/Good/Miss，连击、60% 及格线、音乐同步、暂停/恢复、重试、返回选歌、速度和对音偏移设置。
- 窗口化 Windows 预览：交付/四轨音游/RhythmMVP.exe。
- 原始框架不附带展示谱面；用户已自行导入《漂流人间》，当前保存在 mvp/Assets/StreamingAssets/Songs/chart-1BB56FFFB744。旧方案保留在 backup-original 与 validation。

## 验证

- validation/core-test-result.txt：35 项原有解析与基础判定检查通过。
- validation/rpe-test-result.txt：32 项 RPE、时间换算、四轨映射、文化区域数字解析、导入更新与错误保护检查通过。
- validation/rpe-unity-final.log：最终 Unity 编译与 Windows 构建通过，0 错误；build-result.txt 记录构建摘要。
- validation/runtime-empty/report.json：空曲目列表验证通过。
- validation/runtime-perfect/report.json：7 个隔离测试音符全部 Perfect，暂停测试通过。
- validation/runtime-miss/report.json：7 个隔离测试音符全部 Miss，暂停测试通过。
- validation/runtime-visible：可见窗口下的演奏、暂停和结算截图；preview-visible-ready.png 为默认空列表实际窗口。
- 隐藏窗口运行的较早截图可能为黑色；以 runtime-visible 和实际窗口截图为准。

## 开发位置

- Assets/Scripts/RpeChartLoader.cs：RPE 解析、时间换算、四轨限制。
- Assets/Scripts/ChartImportService.cs：复制 JSON/音频、稳定目录与重复导入。
- Assets/Editor/RpeImportWindow.cs：Unity 用户入口。
- Assets/Scripts/RhythmSession.cs：与画面无关的判定和计分。
- Assets/Scripts/GameManager.cs：播放时钟、输入、菜单、暂停、结算。
- Assets/Scripts/Bootstrap.cs：进入 Play 自动搭建运行对象。
- Assets/Editor/VerifyMvp.cs：验证数据在 validation 下生成，不写入游戏曲目目录。

Unity 包管理在自动化启动环境中曾因缺少 ALLUSERSPROFILE 报 path undefined。tools/Launch.ps1 只为启动的子进程补齐变量，不更改系统设置。Newtonsoft JSON 3.2.1 已作为直接依赖声明。

默认保留之前的 .osu 解析以便旧方案代码兼容，但用户操作入口只展示 RPE JSON 流程，不需要经过 .osu 转换。
## 2026-09-13：按键与 Hold 反馈

用户已验证导入可玩，并明确选择保留 Unity、参考 Phira 改善四轨体验。当前目标仍是把自己制作的一首歌从导入到结算跑通。

- RhythmSession 新增命中头部事件，Hold 开始时反馈、结束时计分，避免重复结算。
- RhythmFeedbackState 独立维护四轨按下、命中、Hold 进度、完成、断开状态；暂停不推进反馈。
- RhythmFeedback 提供轨道/按键亮起、判定色、扩散闪光和小颗粒；Hold 持续脉动、显示进度。原始程序生成的打击音与收尾音通过单独音源播放，菜单可调打击音量。
- NoteController 在 Hold 接住后固定头部、缩短剩余长条，并改变长条颜色。判定文字移至左侧，避免遮住下落音符。
- 当前用户谱面为 9 音符，其中 2 根 Hold，音乐 334.445 秒；本次未代写或修改谱面。
- 参考 Phira 本地 prpr/src/judge.rs 的 Hold 头部即时反馈、prpr/src/core/note.rs 的持续显示，以及 prpr/src/core/resource.rs 的粒子表现思路。Unity 代码与音画素材独立实现，没有复制 Rust 源码或素材。
- 改动前脚本保存在 backup-original/before-feedback-20260913/Scripts。
- 新增菜单“音游 → 验证反馈并构建试玩版”，先停止 Play 使用；不自动关闭 Unity。

验证记录：validation/feedback-core-result.txt（35 项原判定检查、16 项反馈检查）；validation/feedback-build-result.txt（Unity 构建）；validation/feedback-final（真实谱面的全部音符、暂停和曲终检查；判完全部音符后仅跳过无音符尾段）。隐藏验证窗口曾触发 D3D11 呈现报错，改用正常窗口后该报错未复现。较早的 feedback-full-song 和 feedback-visible-song 均在曲终前退出，没有结算报告，不作为完整流程通过的证据；前者有隐藏窗口呈现报错，后者未记录崩溃，退出原因不确定。
最终检查通过：35 项基础解析/判定检查 + 16 项反馈状态检查；最后一次 Unity 构建 Succeeded，0 errors、0 warnings。feedback-final/report.json：9 Perfect、0 Good、0 Miss；9 次头部反馈、2 次 Hold 开始、2 次 Hold 完成、0 次断开；暂停恢复与曲终结算通过。该测试跳过了全部音符结束后的空白音乐尾段，没有验证整段音乐连续播放 334 秒。最终运行日志无异常或显卡呈现报错；用户谱面、音频及其元文件的校验值未变。