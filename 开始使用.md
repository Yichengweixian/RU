# PhiEdit → Unity 四轨音游

游戏框架已经按固定四轨设计。你负责写谱，不用把每首歌写进 C#；每次导出 JSON 后，用 Unity 的导入窗口加入即可。

## 先打开项目

双击本文件夹的“打开 Unity 项目”快捷方式。也可以在 Unity Hub 添加 `mvp` 文件夹，用已安装的 **2022.3.62f3c1** 打开。

第一次等待编译完成。顶部会出现 **音游** 菜单。选择“音游 → 打开游戏场景”，按 Play 进入选歌。目前会列出你已导入的《漂流人间》，可以直接开始试玩。

## 在 PhiEdit 写谱

双击本文件夹的“打开 PhiEdit”快捷方式。

新建你自己的曲目，选择音频，设置 BPM 和音乐偏移。使用一条固定、水平的判定线，音符放在线上方；只使用 **Tap 和 Hold**。不需要配置移动、旋转、父子判定线、假音符、Flick 或 Drag。

横向坐标分为四个区域，从左到右对应 A、S、D、F。建议把音符放在各区域中心，避免误落到相邻轨道。

| 游戏按键 | 建议 X 坐标 | 接受的 X 区域 |
|---|---:|---|
| A | -506.25 | -675 ≤ X < -337.5 |
| S | -168.75 | -337.5 ≤ X < 0 |
| D | 168.75 | 0 ≤ X < 337.5 |
| F | 506.25 | 337.5 ≤ X ≤ 675 |

同一拍在不同区域放音符就是同时按键。同一轨的长按中间不要再放另一颗音符。

保存自己的制谱工程，再导出 **RPE JSON**。它应包含 `META`、`BPMList`、`judgeLineList`。JSON 只记录谱面，音频文件也需要保留；无需转成 .osu。

## 导入并运行

1. 在 Unity 顶部选择 **音游 → 导入 PhiEdit 谱面…**。
2. 选择导出的 JSON；若自动找到配套音频，会帮你填好，否则选择 WAV、OGG 或 MP3。也可以把两个文件拖进窗口。
3. 点击 **导入并运行**，进入游戏测试。

导入器会复制所需文件到项目的 `Assets/StreamingAssets/Songs`，自动记录音频路径。导出的原文件不会被改动。

以后改谱：停止 Unity 的 Play → 在 PhiEdit 导出覆盖同一份 JSON → 回到导入窗口再点“导入并运行”。同一个来源路径会更新原曲目，不会每次新增一首。换了导出路径会作为另一首曲目导入。

也可以在游戏选歌界面点击“导入谱面”，先选 JSON，再按提示选音频。若直接修改项目里的 JSON，点击“刷新列表”。

## 游戏操作

- A / S / D / F：对应四轨；长条按住直到尾端。
- Esc：暂停；Space：继续。切到其他窗口也会暂停。
- 结算页可重试或返回选歌。
- 选歌界面可调下落速度、对音偏移和打击音量。打击音量设为 0 可关闭打击音效；音乐音量不受它影响。先用 0 ms 对音偏移；正偏移使音符相对音乐更晚到线。
- Perfect 为 ±60 ms，Good 为 ±140 ms。长按按一颗音符计分，提前松手会降级或漏击。
- 完成度 =（Perfect + Good × 0.6）÷ 总音符数，60% 及格。

游戏会按 BPM 变化换算节拍，并应用谱面的音乐偏移；显示时采用统一下落速度。制谱器里的预览画面是 Phigros 风格，最终四轨手感请在 Unity 内确认。

### 现在的按键反馈

- 按下任意轨道，对应按键与轨道立即亮起；即使没打中音符也能看见输入响应。
- 打中音符会有短打击音、判定线处的扩散闪光与小颗粒。Perfect 用金色，Good 用蓝色。
- Hold 接住时立即发声并显示“接住”；持续按住时长条变青绿色、判定线持续脉动，下面显示 `HOLD 百分比`。这是该长条的持续进度，不是提前加分。
- Hold 尾端完成后显示“完成”并播放收尾音；提前松开超过容错范围会显示红色“断开”。每根长条仍只结算一次。

更新代码后，先停止旧的 Play，再重新按 Play 试玩。要更新独立程序，退出正在运行的独立程序，在 Unity 选择 **音游 → 验证反馈并构建试玩版**。构建完成后打开下面的 `RhythmMVP.exe`。

## 常见提示

| 提示 | 怎么处理 |
|---|---|
| 不是 RPE JSON | 重新选择 RPE JSON 导出，不要选旧 PEC、Phigros 原生 JSON 或压缩包 |
| 只支持 Tap / Hold | 把 Flick / Drag 改为支持的音符，或删除 |
| 检测到判定线运动 | 将移动事件设为固定值，旋转设为 0，只使用一条带音符的判定线 |
| 同轨重叠 | 将同时出现的音符移到不同区域，避开同轨长按中间 |
| 空谱面 | 可以保存/导入草稿，但至少放一颗音符才能开始 |
| 最后音符超出音频长度 | 检查选错音频、BPM、偏移或长按尾端位置 |

## 文件位置

- `mvp`：Unity 源工程，是后续开发和加歌的主要入口。
- `tools`：制谱器及安装文件。
- `交付/四轨音游/RhythmMVP.exe`：Windows 试玩版，构建时带上 Unity 已导入的曲目；之后在该程序内导入的歌与 Unity 项目分开保存。
- `validation`：技术验证记录和隔离测试数据，不会出现在项目的选歌列表里。
- `backup-original`：旧代码和旧方案备份。

旧的《MVP制作教程》保留供参考；现在以本说明的导入流程为准，不需要手动挂脚本、创建音符预制体或改每首歌的路径。
