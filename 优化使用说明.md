# 四轨音游优化版

项目仍使用 Unity 2022.3.62f3c1。打开原来的 Unity 项目快捷方式，或运行 `交付/四轨音游/RhythmMVP.exe`。

本次新增 Unity Input System 1.7.0，项目启用新旧输入系统并存。已打开的 Unity 编辑器需要保存当前工作并重启一次，让输入后端设置生效。无需更换 Unity 版本。

## 先调手感

选歌页右侧分为“游玩 / 对音 / 练习”三个页签。

- **游玩**：下落速度和打击音量。
- **对音**：全局偏移、当前曲目微调，支持每次 1 ms 或 10 ms，范围 ±300 ms。正值表示音符更晚到达判定线。单曲微调单独保存，不会改动 RPE 文件中的偏移。
- **节拍校准**：跟随听到的声音反复按 A，前四拍用于准备，至少收集 12 次有效敲击，再点击“应用建议”。采用中位数减少离群敲击的影响；结果包含个人习惯和设备延迟，换耳机后建议重新测量。

游玩时显示 Early / Late 和命中误差，结算时显示误差分布。统计针对成功接到的音符头，Hold 尾部不会重复计入。±1 ms 内显示“准时”。

## 练习与暂停

在“练习”页勾选启用练习，输入开始和结束秒数。结束填 0 表示曲终。可选择 0.50～1.50 倍速度以及循环区间；播放速度会改变音调，目前没有保调变速。

练习只包含头尾都落在区间内的完整音符，跨越区间边缘的长按不计入。每轮重新倒数、清空本轮判定，演出状态回到区间起点。练习成绩不保存到正式纪录。

按 Esc 暂停，继续前倒数三秒。暂停时可以松开长按，但需要在倒数结束前重新按住对应键；否则按照暂停位置的长按尾部窗口判定。失去焦点会暂停，倒数中失去焦点会回到暂停页。

## 制作歌曲演出

Unity 顶部菜单 **音游 → 编辑演出时间轴**：选择已经导入的 `chart.json`，添加事件并保存。

事件时间采用音乐播放秒数，与音符判定分开：

| 类型 | 填写内容 | 效果 |
| --- | --- | --- |
| 歌词 | 一段短文字 | 显示在画面下方；空文字清除 |
| 乐器 | 乐器或演奏段落名称 | 显示在左侧提示区；空文字清除 |
| 颜色 | 例如 `#66CCBB` | 改变舞台长按与按键强调色 |
| 动画 | 自定义事件名 | 发送给 `StageTimeline.OnCue`，供角色 Animator 等接收 |

需要接角色动画时，可在场景对象上添加 StageTimeline，并在 Inspector 的 On Cue 中连接接收方法；游戏会优先使用场景中的组件。动态回调的两个参数依次是事件类型和内容。

“演出后记”填写歌曲结束后的文字。正式演奏及格后，结算页显示“进入演出后记”。未填写时保持正常结算。角色美术、具体歌词与剧情内容由你配置，本次没有替你改写现有歌曲。

演出保存在谱面旁边的 `chart.stage.json`，不改变 RPE JSON 或配套音频。重新导入原来的 RPE 谱面时，现有演出文件仍保留。向前跳转会重建歌词、乐器和颜色状态，不补播跳过的动画事件。

## 工程变化

- 判定改为每轨候选索引；密集重叠判定窗口优先命中最早的可判定音符。Hold 接住后必须先完成或松开，才能命中同轨后续音符。
- 按键记录输入事件的按下、松开与原始时间戳，先按时间顺序处理，再推进当前帧的判定。判定窗口在练习变速时保持实际时间 ±60 / ±140 ms。
- 音符与轨道使用独立 Canvas 网格，只提交可见音符及仍显示的长按，不为每颗音符创建或销毁对象。菜单与文字 HUD 保留现有 IMGUI 实现。
- 刷新曲目列表复用未变化谱面的解析结果。同曲重试复用音频，音频修改后重新加载。
- 正式最佳完成度按谱面内容保存；谱面改变后使用新的纪录。偏移与曲目文件互相独立。
- Windows 仍是本次验证平台。

改动前的脚本、项目设置与依赖清单备份在 `backup-original/before-optimization-20260913`。回归与构建记录位于 `validation`；`optimization-check` 是隔离验证副本，日常继续使用 `mvp`。
