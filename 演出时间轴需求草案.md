# 可复用演出时间轴工具 · 需求草案

状态：播放规则已确认，正式素材约定待定。已实现歌词区间、十六分音符循环动画、编辑器以及可选的网格 PNG / Aseprite PNG + JSON 导入，通过自动检查和两首歌曲的实机检查。可选导入能力不代表用户已经选定正式素材规范；最终角色布局与视觉反馈继续待定。本文中的实现建议不自动成为用户确认的需求。

## 已确认的目标与范围

- 继续使用现有 Unity 工程，制作一套可用于多首歌曲的工具。游戏主体框架尚未完善，演出工具应尽量独立、可复用。
- 每首歌曲可以安排歌词和 Aseprite 精灵表动画。
- 歌词由用户填写开始时间、结束时间和文字，系统在指定区间内显示，结束后清除，不要求用户逐画面帧重复配置。
- 动画当前严格跟随歌曲节拍播放，玩家漏按不会打断基础演出。命中、漏按的顿挫、氛围灯光等反馈留待后续设计。
- 动画使用谱面提供的节拍与 BPM 信息，减少逐帧填写时间的工作量。
- 当前选曲基本是 4/4 拍，BPM 大于 100、小于 170。
- 已确认动画每个十六分音符切换一帧，默认循环播放。
- 用户认可在 Unity 内提供演出编辑界面的方向：读取谱面、编辑歌词与动画时间安排、预览结果。

## 已提出的操作方案（具体行为待最终确认）

1. 选择已经导入的曲目，读取 RPE JSON 中的 BPM 和音符拍点。
2. 在歌词轨道输入开始/结束时间，例如 `01:12.500—01:16.000`，填写该区间显示的整句歌词。
3. 在动画轨道选择精灵表，设置开始和结束的小节/拍点；按已确认的每个十六分音符一帧、默认循环规则播放。
4. 时间轴显示秒数与小节刻度，支持填写精确数值、拖动播放位置预览。
5. 每首歌保存独立演出配置，动画素材可用于多首歌曲；不改写原谱面的音符。
6. 暂停、练习变速和跳转时，歌词及动画直接根据歌曲当前位置取样，避免累计计时误差。

## 动画换帧与循环：已确认

- 每个十六分音符切换一帧。4/4 拍每小节换帧 16 次；BPM 100～170 对应约 6.7～11.3 次/秒。
- 动画默认循环。例如 8 帧动画按该规则播放，一轮占半小节；16 帧动画一轮占一小节。循环长度由素材帧数决定，帧数不要求一律为 16。
- 换帧间隔随歌曲 BPM 改变，保持音乐拍点同步。
- 以上规则控制动画姿势的更替；游戏输入、判定和音符下落继续正常更新。
- “每个十六分音符两帧”不纳入当前已确认需求；播放一次并停在最后一帧等扩展方式也尚未确认。

## 后续需要明确的素材与编辑约定

用户说明素材尚未开工，素材制作及导出约定暂不确定，待有具体制作需求或样例后再讨论。

- 精灵表采用规则网格 PNG，还是 PNG 加 Aseprite 导出的 JSON 元数据；是否需要支持同一精灵表中的多个动画标签。
- 一首歌中是否同时显示多个角色/动画控件，以及是否需要直接调整位置、大小和遮挡顺序。
- 动画片段结束时的显示状态，以及歌词重叠区间的处理。

这些细节不应从助手建议中自动视为用户已确认。

## 拟定验收场景

- 同一套系统在两首不同 BPM 的曲目中使用，各自保存与读取歌词、动画安排。
- 歌词在开始时间出现、结束时间消失；暂停和跳转后仍显示正确内容。
- 导入精灵表后，能在指定小节/拍点显示对应帧。
- 变 BPM 时仍保持选定的十六分音符换帧规则。
- 暂停不推进动画；恢复、练习变速和跳转不造成动画与歌曲错位。
- 漏按不会中断当前基础动画；后续反馈可以独立扩展。
- 编辑器预览与实际游玩采用相同时间换算和帧选择规则。

以上为待确认后的验收候选项，尚不是完成声明。
