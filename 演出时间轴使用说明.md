# 可复用演出时间轴

已确认的规则：每个十六分音符换一帧，默认循环。4/4 拍中，8 帧动画半小节循环一次，16 帧动画一小节循环一次。帧数指不同动画画面数量，与 Unity 渲染帧率独立。

## 当前操作

1. 在 Unity 打开 `音游 → 编辑演出时间轴`，选择已导入曲目的 RPE 谱面 JSON。
2. 在“歌词区间”填写开始时间、结束时间和整句文字。时间格式为 `01:12.500`。到开始时间显示，到结束时间隐藏。
3. 在“动画片段”添加循环动画，填写控件名、开始与结束拍点。格式为 `小节:拍:细分`，小节和拍从 1 开始，细分为 0～3。例如 `1:1:0` 至 `2:1:0` 为第一小节。
4. 没有素材时选择占位动画、设置占位帧数；有素材时点击“导入精灵表”，完成导入后在“动画名称”下拉框选择动画。播放按钮、时间输入和进度条可以检查编排；音频已导入 Unity 时支持预听，否则显示静音预览提示。
5. 保存后，配置写入谱面旁的 `.stage.json` 文件，下一次演奏时读取。导出模板后可用于其他曲目：动画保留拍点，歌词保留秒数，需要按新歌调整。

歌词使用音乐秒数，动画使用谱面 BPM、偏移与游戏判定时间；暂停、练习变速和跳转时直接根据当前位置选择画面。基础动画不依赖命中结果。

## 精灵表导入

两种入口都是可选能力，不要求现在决定正式素材的制作方式。

- **Aseprite PNG + JSON**：选择图片和配套帧数据，自动读取帧顺序、裁剪位置和动画标签。支持 JSON Array 与 JSON Hash，支持正序、倒序和往返标签。裁掉的透明边缘会还原到原始画布，避免动画因裁剪而偏移。
- **规则网格 PNG**：填写单帧宽高、有效帧数；有边距或间距时填写对应像素数。按左上到右下、逐行读取，不要求素材一律 16 帧。

Aseprite 可以同时导出精灵表图片和 JSON 帧数据，见[官方导出文档](https://www.aseprite.org/docs/cli/#data)。当前导入器使用 1 倍尺寸、无内部填充的帧数据；旋转打包或尺寸不匹配时会给出错误提示。Aseprite 的每帧毫秒数与标签重复次数不用于计时，音乐中的每个十六分音符始终推进一个序列位置，片段内循环。

点击“导入为新素材库”会生成独立 Unity 资源，保留原 PNG 和 JSON。重复导入会生成新库；已有曲目继续引用原库，需主动切换才会使用新版本。在其他歌曲的动画轨道选择同一个库即可复用。素材导入 Unity 后，重新构建程序即可随游戏打包；运行中的已发布程序不负责导入美术源文件。

示例位于 `mvp/Assets/StageSamples`：`song1.json` 与 `song2.json` 使用不同 BPM，共用一套已导入动画。可直接在演出编辑器选择这两个谱面并拖动预览。`demo.png` 和 `demo.json` 是程序生成的导入测试素材，不是正式美术。

## 当前边界

- 正式素材制作规范和最终角色布局继续待定；上述两种导入方式已经可用。
- 临时实现约定：动画片段结束后隐藏；歌词重叠、同一控件动画片段重叠会提示错误。这些约定仍可在设计讨论中调整。
- 正式场景可通过 `StageSpriteTarget` 绑定 SpriteRenderer 或 UI Image，控件名与动画片段一致即可。当前游戏右侧的小型预览用于确认画面与时间，不代表最终舞台布局。
- 编辑器功能已做自动集成检查；完整人工操作体验和未来正式素材的视觉效果仍需在后续使用中确认。

## 本次验证

`validation/stage-timing-result.txt` 记录了 122 项时间轴检查通过，覆盖歌词边界、BPM 变化、循环、跳转、暂停、变速、配置保存及 Sprite 序列绑定。此前的核心与反馈检查也通过。

另外通过 15 项编辑器检查，覆盖加载、精确跳转及边界限制、保存与重新加载、无效配置保护。切换到无效配置时保留当前未保存内容；保存无效内容时保留磁盘上的原配置。这些检查调用编辑器实际功能，但不替代人工操作和视觉验收。

精灵表新增 44 项检查通过，覆盖网格、两种 JSON、标签顺序、裁剪还原、像素方向、资源保存、SpriteRenderer / UI Image 接入，以及两首不同 BPM 歌曲的复用。

最新 Unity 构建成功，0 错误、1 项无图形批处理环境的环境光探针警告，详见 `validation/stage-timing-result.txt` 和 `validation/build-messages.txt`。

构建后的程序使用附带演出配置的测试曲目完成一轮自动演奏，7 个音符全部 Perfect，暂停检查通过（`validation/stage-timing-runtime/report.json`）。该检查验证运行接入，不替代画面和编辑器交互验收。原歌曲的 5 个文件校验值均未改变。

导入功能完成后，又运行了两首使用真实 Sprite 资源的测试曲目：第一首 7 个音符全部 Perfect，第二首 7 个音符全部 Miss。每首均命中 6 个预先定义的歌词与动画验证时间窗口，0 偏差，并记录到暂停时画面保持不变。报告分别位于 `validation/stage-sheet-runtime-song1` 与 `validation/stage-sheet-runtime-song2-miss`。已查看第一首试玩截图，精灵显示正常。
